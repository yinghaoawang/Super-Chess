Makefile provided by Joseph Anderson

When building, make sure there is a "bin/" directory in the current directory

After building with "make"
Run with "./run"

Or with java -Dfile.encoding="UTF-8" -cp ./bin ChessGameApp
* on windows, remove the -Dfile option, but the chess pieces will not display properly *
